{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Build Schema",
  "$ref": "#/definitions/build",
  "definitions": {
    "build": {
      "type": "object",
      "properties": {
        "AutoDetectBranch": {
          "type": "boolean",
          "description": "Whether to auto-detect the branch name - this is okay for a local build, but should not be used under CI"
        },
        "AzureKeyVaultAppId": {
          "type": "string",
          "description": "AzureKeyVaultAppId"
        },
        "AzureKeyVaultAppSecret": {
          "type": "string",
          "description": "AzureKeyVaultAppSecret"
        },
        "AzureKeyVaultCertificateName": {
          "type": "string",
          "description": "AzureKeyVaultCertificateName"
        },
        "AzureKeyVaultUrl": {
          "type": "string",
          "description": "AzureKeyVaultUrl"
        },
        "BuildVerbosity": {
          "type": "string",
          "description": "Build verbosity"
        },
        "CALAMARI_CurrentBranch": {
          "type": "string",
          "description": "Branch name for Calamari to use to calculate the version number. Can be set via the environment variable CALAMARI_CurrentBranch"
        },
        "Configuration": {
          "type": "string",
          "description": "Configuration to build - Default is 'Debug' (local) or 'Release' (server)",
          "enum": [
            "Debug",
            "Release"
          ]
        },
        "Continue": {
          "type": "boolean",
          "description": "Indicates to continue a previously failed build attempt"
        },
        "Help": {
          "type": "boolean",
          "description": "Shows the help text for this build assembly"
        },
        "Host": {
          "type": "string",
          "description": "Host for execution. Default is 'automatic'",
          "enum": [
            "AppVeyor",
            "AzurePipelines",
            "Bamboo",
            "Bitrise",
            "GitHubActions",
            "GitLab",
            "Jenkins",
            "Rider",
            "SpaceAutomation",
            "TeamCity",
            "Terminal",
            "TravisCI",
            "VisualStudio",
            "VSCode"
          ]
        },
        "NoLogo": {
          "type": "boolean",
          "description": "Disables displaying the NUKE logo"
        },
        "PackInParallel": {
          "type": "boolean",
          "description": "Run packing step in parallel"
        },
        "Partition": {
          "type": "string",
          "description": "Partition to use on CI"
        },
        "Plan": {
          "type": "boolean",
          "description": "Shows the execution plan (HTML)"
        },
        "Profile": {
          "type": "array",
          "description": "Defines the profiles to load",
          "items": {
            "type": "string"
          }
        },
        "Root": {
          "type": "string",
          "description": "Root directory during build execution"
        },
        "SignBinaries": {
          "type": "boolean",
          "description": "Sign Binaries"
        },
        "SigningCertificatePassword": {
          "type": "string",
          "description": "SigningCertificatePassword"
        },
        "SigningCertificatePath": {
          "type": "string",
          "description": "SigningCertificatePath"
        },
        "Skip": {
          "type": "array",
          "description": "List of targets to be skipped. Empty list skips all dependencies",
          "items": {
            "type": "string",
            "enum": [
              "BuildCI",
              "BuildLocal",
              "CheckForbiddenWords",
              "Clean",
              "Compile",
              "CopyToLocalPackages",
              "Pack",
              "PackBinaries",
              "PackTests",
              "Restore",
              "SetOctopusServerVersion",
              "SetTeamCityVersion"
            ]
          }
        },
        "Solution": {
          "type": "string",
          "description": "Path to a solution file that is automatically loaded"
        },
        "Target": {
          "type": "array",
          "description": "List of targets to be invoked. Default is '{default_target}'",
          "items": {
            "type": "string",
            "enum": [
              "BuildCI",
              "BuildLocal",
              "CheckForbiddenWords",
              "Clean",
              "Compile",
              "CopyToLocalPackages",
              "Pack",
              "PackBinaries",
              "PackTests",
              "Restore",
              "SetOctopusServerVersion",
              "SetTeamCityVersion"
            ]
          }
        },
        "Verbosity": {
          "type": "string",
          "description": "Logging verbosity during build execution. Default is 'Normal'",
          "enum": [
            "Minimal",
            "Normal",
            "Quiet",
            "Verbose"
          ]
        },
        "where": {
          "type": "string",
          "description": "Test filter expression"
        }
      }
    }
  }
}